# macOS
.DS_Store

# Xcode / XcodeGen
ImageUpscaler.xcodeproj/
build/
DerivedData/
*.xcresult
.xcode.env.local
*.xcuserstate

# RealESRGAN.mlpackage IS committed (BSD-3-Clause, see Models/README.md) —
# but Xcode's own compiled-model output isn't source, so ignore that.
Models/*.mlmodelc

# Swift / SPM
.build/
*.swp
.swiftpm/

# Editor
.idea/
